# Authentication Module using shinyOAuth

OAuth2/OIDC authentication module for nemetonApp. Supports Keycloak,
Google, GitHub, Microsoft, and any OIDC provider. Authentication is
optional: if no OAuth credentials are configured, the app runs without
authentication (mode anonyme).
