% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/service_r5.R
\name{add_r5_to_indicators}
\alias{add_r5_to_indicators}
\title{Enrich the radar indicator sf with a live R5 dépérissement column}
\usage{
add_r5_to_indicators(base_sf, project)
}
\arguments{
\item{base_sf}{The per-UGF indicator `sf` (`project$indicators_sf`).}

\item{project}{The current project (reads `metadata$monitoring_zone_id`).}
}
\value{
`base_sf`, possibly with an added `indicateur_r5_deperissement`
  column (NA on UGFs with no intersecting alert / no routed method).
}
\description{
Best-effort: returns `base_sf` unchanged when there is no bound
monitoring zone, no reachable DB, or no dieback alert. The added
column is named `indicateur_r5_deperissement` (raw, "high = dieback")
so `nemeton::create_family_index()` detects it via the core
`INDICATOR_FAMILIES$R` config and inverts it for the radar.
}
\keyword{internal}
